local std = {
    -- These globals can be set and accessed.
    globals = {
        "awful",
        "beautiful",
        "cwd"
    },
    -- These globals can only be accessed.
    read_globals = {
        "screen",
    }
}
